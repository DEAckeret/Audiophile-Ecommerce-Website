import Alert from 'react-bootstrap/Alert';

function Example() {
  return (
    <Alert variant="success">
      This is a success alert with React Bootstrap!
    </Alert>
  );
}

export default Example;